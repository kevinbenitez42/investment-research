# Risk Analysis Dashboard

Install the dashboard dependencies:

```bash
pip install -e .[dashboard]
```

Run the app from the repository root:

```bash
python -m dashboards.risk_analysis.app
```

The dashboard reuses the `Quantapp` analytics and visualization modules and is intended to be the app counterpart to `Notebooks/Single Asset Profile/Risk Analysis .ipynb`.
