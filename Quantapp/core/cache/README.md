# Cache

Caching interfaces and generic cache implementations live here.

This layer should define shared cache abstractions and reusable implementations that can be used by data, services, workflows, and application surfaces.

