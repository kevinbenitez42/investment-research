# Config

Application configuration infrastructure lives here.

This layer should manage environment settings, runtime configuration, and shared application options that are not specific to one feature area.

