# Exceptions

Common exceptions and error base classes live here.

This layer should define reusable exception types for infrastructure and application-level failures when a standard Python exception is not expressive enough.

