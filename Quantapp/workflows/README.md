# Workflows

This package contains higher-level flows that assemble data, analytics, and visualization into reusable payloads.

Use a workflow when you want something that is:

- richer than a single helper function
- reusable outside a notebook
- suitable for application or dashboard consumption

Current notable file:

- `risk_analysis_dashboard.py`: assembles a reusable risk-analysis dashboard payload from shared components
