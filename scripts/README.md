# Scripts

This folder contains helper scripts for repository maintenance and local workflows.

Current script:

- `clear_notebooks.sh`: utility for clearing notebook outputs

Keep this folder focused on small operational helpers rather than reusable library code.
